Completely standalone receipt app written in HTML, JavaScript and CSS.

Things that is required for this to work as intended:

1) Setup printer on localhost
2) Make sure you run chrome, this has not been tested with any other browser.
3) Open the printing options in chrome, ctrl+p, and tick off "Headers and footers"
4) Run it in fullscreen by pressing F11
